package org.paulvargas.webapp.dao;

public interface Common {
	/**
	 * Returns the current date as string in format {@code dd/MM/yyyy}.
	 *
	 * @return the current date in format {@code dd/MM/yyyy}.
	 */
	String getCurrentDate();
}
